name: "Basic VPC Example"
schema_version: "1.0.0"
description: "Simple two module Vpc blueprint. Deploys VPC resources and COS bucket"
inputs:
  - name: resource_group_name
  - name: vpc_region
  # - name: cos_instance_name
  # - name: create_bastion
  - name: name
outputs:
  - name: cos_id
    value: $module.bp-rt-v5-cos-workspace.outputs.cos_id
  - name: vpc_id
    value: $module.bp-rt-v5-vpc-workspace.outputs.vpc_id
settings:
  - name: TF_VERSION
    value: 1.0
modules:
  - name: bp-rt-v5-vpc-workspace
    layer: VPC
    source:
      git: 
        source_type: github
        git_repo_url: "https://github.com/cloud-design-dev/example-blueprint/tree/main/modules/vpc"
        git_branch: main
    inputs:
      - name: name
        value: $blueprint.name
      - name: vpc_region
        value: $blueprint.vpc_region
      - name: resource_group_name
        value: $blueprint.resource_group_name
    outputs:
      - name: vpc_id
      - name: frontend_subnet_id
      - name: backend_subnet_id
      - name: vpc
  - name: bp-rt-v5-cos-workspace
    layer: COS
    source:
      git:
        source_type: github
        git_repo_url: "https://github.com/cloud-design-dev/example-blueprint/tree/main/modules/cos"
        git_branch: main
    inputs:
      - name: name
        value: $blueprint.name
      - name: resource_group_id
        value: $workitem.bp-rt-v5-vpc-workspace.outputs.resource_group_id
      - name: vpc_region
        value: $blueprint.vpc_region
    outputs:
      - name: cos_id
      - name: backend_collector_bucket
      - name: frontend_collector_bucket

  - name: bp-rt-v5-flowlogs-workspace
    layer: Metrics
    source:
      git:
        source_type: github
        git_repo_url: "https://github.com/cloud-design-dev/example-blueprint/tree/main/modules/collector"
        git_branch: main
    inputs:
      - name: name
        value: $blueprint.name
      - name:  resource_group_id
        value: $workitem.bp-rt-v5-vpc-workspace.outputs.resource_group_id
      - name: frontend_subnet_id
        value: $module.bp-rt-v5-vpc-workspace.outputs.frontend_subnet_id
      - name: backend_subnet_id
        value: $workitem.bp-rt-v5-vpc-workspace.outputs.backend_subnet_id
      - name: backend_collector_bucket
        value: $workitem.bp-rt-v5-cos-workspace.outputs.backend_collector_bucket       
      - name: frontend_collector_bucket
        value: $workitem.bp-rt-v5-cos-workspace.outputs.frontend_collector_bucket 